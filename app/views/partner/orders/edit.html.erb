<h1><%= t('.order_edit_header') %></h1>

<%= render 'form', order: @order %>

<%= link_to t('.order_detail_link'), partner_shop_order_path(@order.shop, @order) %> |
<%= link_to t('.back_link'), partner_shop_orders_path %>
